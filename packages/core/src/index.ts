/**
 * @riff-line/core — Webコア本体（L1〜L3）。
 * 本パッケージ「Webコア基盤構築」で確立するのは rendering（alphaTab ファサード）と
 * platform（PlatformAdapter I/F の最小契約）のみ。
 */

export * from './rendering';
export * from './platform';
