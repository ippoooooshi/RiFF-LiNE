# テスト方針設計書

対応要件：要件定義書5.5節（テスト方針）、9章「自動テスト基盤」「開発用サンプルデータ」作業パッケージ、8章リスク#5

**2026-09-02追記（品質KPIの集約、本人の指摘を受けて）**：本人より「テストも設計も、一人だからとかは関係なく品質だけが優先。KPIを設定してそれに準ずる形にするのが一番」との方針が示された。[[15_development_process.md#0]]で是正した通り、本プロジェクトは既に「一人開発・隙間時間開発」を理由に自動化された工程（Lint強制・セルフレビュー範囲等）を緩めない方針に転換している。0節を新設し、その方針を「テスト」だけでなく「設計」にも一貫させる形で、達成すべき品質基準を曖昧な言葉ではなく具体的な数値（KPI）として一箇所に集約した。あわせて10節（継続的インテグレーション）を、[[15_development_process.md#2]]の是正内容と整合する形に書き直した。本書の題名は「テスト方針設計書」だが、0節はその範囲を設計側のKPIにも広げている——テストと設計の品質を同じ土俵で扱うべきという本人の方針を反映したものであり、[[12_traceability.md]]・[[13_design_decision_points.md]]と役割が重複しないよう、0節はあくまで「数値目標の集約先」に限定し、判定プロセスそのものは従来通り各文書（12番・13番）に委ねる。

## 0. 品質KPIと本書の位置づけ（2026-09-02追加）

### 0.1 品質KPI一覧

以下は、本プロジェクトが達成すべき品質基準を、テスト・性能・設計の領域を横断して数値・チェック項目として一覧化したものである。個々の数値の定義元・実際にチェックが行われる場所（ゲート）を併記する。

| 領域 | KPI | 目標値 | 定義元 | ゲート |
|---|---|---|---|---|
| 単体テストカバレッジ | C0（命令網羅） | 実装した全メソッドで100% | 本書2節 | CI（[[15_development_process.md#2]]）・DoD基準3（[[15_development_process.md#7]]） |
| 単体テストカバレッジ | C1（分岐網羅） | 実装した全メソッドで100% | 本書2節 | 同上 |
| 単体テストカバレッジ | C2（条件網羅） | 本書2節の対象ロジック一覧で100% | 本書2節 | 同上 |
| Lint／型チェック | ESLint違反・`tsc --noEmit`の型エラー | 0件 | [[15_development_process.md#2]] | pre-commit hook＋CI |
| コミット規約 | commitlint違反 | 0件 | [[15_development_process.md#3.2]] | pre-commit hook |
| 起動時間 | 起動〜曲一覧表示 | 3秒以内 | [[09_nonfunctional.md#1]] | 8節の負荷テスト |
| 入力反応 | ノート入力〜画面反映 | 100ms以内 | [[09_nonfunctional.md#1]] | 同上 |
| 描画性能 | フレームレート | 30fps以上 | [[09_nonfunctional.md#1]] | 同上 |
| 再生開始 | 再生ボタン〜発音 | 200ms以内 | [[09_nonfunctional.md#1]] | 同上 |
| メモリ使用量 | アイドル時 | 150MB以内 | [[09_nonfunctional.md#3]] | 同上 |
| メモリ使用量 | 通常編集時（**1編集ウィンドウあたり**、2026-09-03明確化） | 400MB以内 | [[09_nonfunctional.md#3]] | 同上 |
| メモリ使用量 | 大曲（2048小節）編集時（**1編集ウィンドウあたり**、2026-09-03明確化） | 600MB以内 | [[09_nonfunctional.md#3]] | 同上 |
| Undo/Redo履歴（2026-09-03追加） | メモリ予算（1編集ウィンドウ＝`CommandHistory`インスタンスあたり） | 80MB以内。ただし直近200件は予算超過でも下限保証 | [[13_design_decision_points.md#4]]C11 | 単体テスト（本書2節C2対象ロジック）＋8節の負荷テスト |
| セルフレビュー実施率 | 完了した作業パッケージのうちセルフレビューを実施したものの割合 | 100%（サイズ問わず） | [[15_development_process.md#6]] | DoD基準4 |
| 要件トレーサビリティ | 要件定義書の機能要件のうち基本設計いずれかの文書にマッピング済みのものの割合 | 100% | [[12_traceability.md]] | 基本設計完了基準（[[12_traceability.md#5]]） |
| 設計分岐点の未決着（カテゴリB／C） | 当該Phaseの詳細設計着手時点での工学判断／ヒアリング確定事項の未決件数 | 0件（着手前に必ず決着） | [[13_design_decision_points.md#5]] | 各作業パッケージの詳細設計作成（DoD基準2の前提） |
| 設計分岐点の未決着（カテゴリA） | 当該Phaseのシステムテスト実施時点での技術検証待ち事項のうち、フォールバックが未確定のものの件数 | 0件 | [[13_design_decision_points.md#2]] | 本書6節システムテスト |
| ドキュメント同期 | セルフレビュー等で発見される基本設計／詳細設計／実装間の不整合の残存件数 | 発見都度ゼロ化（残存させない） | [[15_development_process.md#9]] | セルフレビュー観点（[[15_development_process.md#6]]） |

### 0.2 運用原則

- **目標値そのものは、一人開発・隙間時間開発であることを理由に下げない。** 未達が判明した場合に見直すのは目標値ではなく設計・実装である（例：[[09_nonfunctional.md#2]]で示した「メモリ超過時は遅延デシリアライズ等の追加対策を検討する」という対応方針が、目標値を動かすのではなく実装側で目標値に合わせにいく例にあたる）。
- 上記KPIのうち自動測定可能なもの（カバレッジ、Lint、コミット規約）はCI（[[15_development_process.md#2]]）でPRごとに機械的に検証する。手動測定が必要なもの（起動時間・fps等の性能、メモリ使用量）は8節の負荷テストで、Phase 0/1着手後に実機で計測する。
- 設計側のKPI（要件トレーサビリティ、設計分岐点の未決着件数）はコードのように機械計測できないため、[[12_traceability.md#5]]の完了基準チェックリストと[[13_design_decision_points.md#5]]の運用ルールを通じて、各文書の更新のたびにClaude Codeのセルフレビュー・本人のレビューが確認する。
- 本表は今後、新しい品質観点が確定するたびに追記する（例：Phase 2エクスポート性能のA10、Phase 3のA11検証結果が確定した際の追加KPI）。KPIの追加・変更自体は[[13_design_decision_points.md]]の分岐点としては扱わず、本節の直接更新でよい——KPIは分岐点そのものではなく、分岐点の検討を経て確定した数値の集約先だからである。

## 1. テストレベル構成（本セッションで確定）

次の詳細設計・その後の実装フェーズを通じて、以下の4段階のテストレベルをこの順で積み上げる方針とする。下位レベル（単体）で保証しきれない範囲を上位レベルが補う構造であり、いずれか1段階だけで品質を担保しようとしない。

| レベル | 対象粒度 | 目的 | 実施タイミング |
|---|---|---|---|
| 単体テスト | メソッド単位 | 個々のロジックの正しさを、コードカバレッジ基準（2節）で保証する | 各作業パッケージの実装直後（[[15_development_process.md#5]]） |
| 結合テスト | モジュール単位（サービス層をまたぐ組み合わせ） | サービス間の連携・データの受け渡しが設計通りに機能することを確認する | 単体テスト完了後、作業パッケージの完了前後 |
| システムテスト | アプリ全体（実際の操作シナリオ） | 要件定義書のユースケースがエンドツーエンドで成立するかを確認する | 主要な作業パッケージが揃ったタイミング、Phase節目 |
| 運用テスト | 実際の耳コピ作業（本番相当の使い方） | 実利用に耐えるか、実運用フローでの違和感がないかを確認する | Phase 1完了前後、以降の主要機能追加時 |

以降の節でレベルごとの詳細を定義する。

## 2. 単体テスト：カバレッジ方針（本セッションで新規確定）

次の詳細設計・その後の実装で作成する各メソッドについて、以下のカバレッジ基準を単体テストの完了条件とする。

| カバレッジ種別 | 適用範囲 | 基準 |
|---|---|---|
| C0（命令網羅） | 実装した全メソッド | **100%**。全ての実行文が最低1回は実行されること |
| C1（分岐網羅） | 実装した全メソッド | **100%**。if／switch／三項演算子等の全分岐が真偽双方で最低1回は実行されること |
| C2（条件網羅） | 下記「対象ロジック」に限定 | 複合条件（`&&`／`||`で結合された条件式）を構成する各要素が、真偽の組み合わせで検証されること |

**C2を適用する対象ロジック**（本セッションで確定。以後、新たに複合条件を持つ重要ロジックを追加した場合はこの一覧に追記する）：

- コマンド層のexecute／undo（[[04_editing_core.md#8]]）：Undo/Redoの非対称性はデータ消失に直結するため
- Undo/Redo履歴のメモリ予算判定・下限保証・エビクション（[[04_editing_core.md#8.2]]、[[../detailed_design/editing-core.md#6.2]]、2026-09-03追加）：予算超過×下限件数の複合条件のため
- バリデーションサービスの各種上限・重複判定（[[04_editing_core.md#11]]、[[02_data_model.md#4.3]]）
- 奏法記号の自動候補判定（[[04_editing_core.md#6.1]]）：間隔・フレット差の複合条件
- 拍子／テンポの継承チェーン再計算（[[04_editing_core.md#10]]、[[13_design_decision_points.md#3]]B5）
- スキーマバージョン間マイグレーション（[[02_data_model.md#4.2]]）：データ消失防止が最優先のため
- ファイルI/Oのアトミック書き込み・整合性検証（[[06_file_io_persistence.md#5]]）
- `NotificationCenter`のレベル別ルーティング判定（[[08_error_logging.md#1.1]]）：Info/Warning/Error/Criticalの振り分け条件（4節参照）
- フォーカスビューの表示範囲追従判定（[[../detailed_design/view-modes.md#4.1]]、[[../detailed_design/view-modes.md#6]]、2026-09-09追加）：モードが`focus`である条件とカーソルが表示範囲（`start ≤ barIndex ≤ end`）を外れた条件の複合のため

上記以外（画面表示コンポーネントの見た目分岐、単純なゲッター／セッター等）はC0/C1のみを満たせばよく、C2までは求めない。この「全メソッドでC0/C1を100%、重要ロジックはC2まで」という基準は、要件5.5「データモデル・Undo/Redo等の主要ロジックは自動テスト化」を具体的な数値基準まで踏み込んで確定させたものであり、実施タイミングは[[15_development_process.md#5]]（主要ロジック実装後にまとめて書く。L/XLサイズはサブ機能単位で区切る）に従う。数値目標は0節の品質KPI一覧にも集約している。

## 3. 単体テスト対象一覧（詳細）

| 対象 | テスト種別 | カバレッジ目標 | 優先度 |
|---|---|---|---|
| コマンド層（[[04_editing_core.md#8]]）：各Commandのexecute/undoの対称性 | 単体テスト | C0/C1/C2 | 最高。Undo/Redoの信頼性は要件上の必須事項 |
| バリデーションサービス（[[04_editing_core.md#11]]）：各上限値・重複チェック | 単体テスト | C0/C1/C2 | 高 |
| 奏法記号の自動候補判定ロジック（[[04_editing_core.md#6.1]]） | 単体テスト | C0/C1/C2 | 中 |
| 拍子／テンポの継承チェーン再計算（[[04_editing_core.md#10]]） | 単体テスト | C0/C1/C2 | 高 |
| コードネーム自動検出ロジック（[[04_editing_core.md#7]]） | 単体テスト（既知コード進行のフィクスチャで検証） | C0/C1 | 中 |
| スキーマバージョン間マイグレーション（[[02_data_model.md#4.2]]） | 単体テスト（旧バージョンJSON→新バージョンの変換結果検証） | C0/C1/C2 | 高。データ消失は致命的なため |
| MIDIシリアライザ（自前実装、[[07_export_print.md#1]]） | 単体テスト＋ラウンドトリップテスト（変換後に再読み込みして元データと比較） | C0/C1 | 高 |
| alphaTexエクスポート連携（**2026-09-01更新**：ネイティブAPI `AlphaTexExporter`利用、[[07_export_print.md#1]]、[[13_design_decision_points.md#2]]A3） | 単体テスト（エクスポート結果をalphaTexとして再インポートし元Scoreと比較するラウンドトリップ検証＋エクスポート設定オプションの妥当性確認）。自作シリアライズロジックではなくライブラリ呼び出しの正しさの検証が中心 | C0/C1 | 中（自作ロジックの網羅ではないため優先度をやや引き下げ） |
| `NotificationCenter`のレベル別ルーティング（[[08_error_logging.md#1.1]]、4節参照） | 単体テスト（`report()`直接呼び出し） | C0/C1/C2 | 高（本セッションで追加） |
| ファイルI/Oのアトミック書き込み（[[06_file_io_persistence.md#5]]） | 単体テスト（整合性検証ロジック部分）＋結合テスト（実I/O、5節） | C0/C1/C2 | 中 |
| 上記以外の全メソッド | 単体テスト（悉皆） | C0/C1 | - |

## 4. UIエラー方針のテスト可能性（本セッションで新規確定）

[[08_error_logging.md#1]]で定義した4段階（Info/Warning/Error/Critical）のメッセージ方式は、以下の設計により単体・結合テストで再現・検証できるようにする。

- `NotificationCenter`はWebコア内の純粋ロジックとして実装し、Electron・DOM等のプラットフォーム固有APIに依存しない（[[01_architecture.md]]のレイヤー依存規則にも合致）。単体テストから`report(code, context)`を直接呼び出し、正しいレベル（トースト／ハイライト／モーダルのいずれに振り分けられるか）・正しいログ記録が行われるかを検証できる。**2026-09-02修正**：本節はかつて`report(level, code, context)`という3引数の呼び出し例を記載していたが、[[../detailed_design/error-logging-foundation.md#2.1]]で確定した実際のシグネチャは`report(code, context)`（レベルは`ErrorCodeRegistry`がコードから導出）であるため、記載を実装と一致させた（セルフレビューで発見）。
- 各エラーコード（[[08_error_logging.md#1.2]]の`{ドメイン}-{連番}`形式）ごとに、そのコードを発生させる最小限の入力（例：`EDIT-001`なら重複配置操作）を単体テストのフィクスチャとして用意し、実際に該当コードで`report()`が呼ばれることを確認する。
- Criticalレベル（データ破損検知・クラッシュ復旧確認等）は実際の破損状態を単体テストで再現しにくいため、ファイルI/Oのフォールトインジェクション（5節、書き込み中断のシミュレーション）やチェックサム不一致を意図的に注入したフィクスチャファイルを使い、結合テストレベルで再現する。
- 表示コンポーネント（トースト・ハイライト・モーダルのReact実装）は、`NotificationCenter`からのイベントを受けて正しい見た目を描画するかをコンポーネント単体テストで検証する。ロジック（いつ・どのレベルで通知するか）と表示（どう見せるか）を分離しているため、両者は独立にテスト可能である。

## 5. 結合テスト（モジュール単位、本セッションで新規確定）

単体テストで検証済みのモジュール同士を組み合わせ、サービス層をまたぐ連携が設計通りに機能することを確認する。

| 対象 | 確認内容 |
|---|---|
| 編集サービス＋コマンド層＋ドメインモデル | UIイベント相当の入力から、コマンド発行→モデル更新→カーソル移動までの一連の流れ（[[04_editing_core.md#1]]） |
| 再生サービス＋alphaTab（AlphaSynth）統合 | 合奏再生・ソロ・ループ境界での部分差し替え（[[05_playback_audio.md#3.2]]）が実際のalphaTab APIと噛み合うか |
| ファイルI/O＋ストレージバックエンド切替 | ローカル／iCloud Drive／Google Driveそれぞれのフォルダに対する読み書き、保存先移行フロー（[[06_file_io_persistence.md#2.2]]） |
| エラー通知経路 | 各サービス層でのエラー発生→`NotificationCenter`→ログ記録＋UI表示要求までの一連の経路（4節で単体テスト済みのルーティングを、実際の呼び出し元サービスと組み合わせて再検証） |
| スキーママイグレーション＋ファイル読み込み | 旧バージョンの`.tabapp`ファイルを実際に読み込ませ、マイグレーション後に正しく編集・保存できるか |

## 6. システムテスト

Claude Codeによる操作シナリオベースの通し確認を、要件定義書のユースケース単位で行う（例：「4/4拍子の8小節をステップ入力し、途中でタイ・チョーキングを付与し、Undo/Redoで意図通り戻ることを確認する」「曲を新規作成→パート追加→合奏再生→エクスポートまでの一連の流れ」）。実際にElectronアプリを起動した状態で確認し、単体・結合テストでは見えないUI経由の不具合（ショートカット競合、レイアウト崩れ、[[14_visual_design_system.md#8]]のレスポンシブ原則の破綻等）を洗い出す。主要な作業パッケージが揃ったタイミング、およびPhase節目で実施する。

## 7. 運用テスト

人間（開発者本人）による実演奏との突き合わせテストを運用テストと位置づける。実際の耳コピ作業フローを通しで行い、UX上の違和感（要件4.5の複数操作パターン対応が機能しているか等）や、システムテストまでの工程では気づけない実運用上の課題を確認する。Phase 1完了前後、および以降の主要機能追加時に実施する。

## 8. 負荷テスト計画（要件9章Phase 1後半、リスク#5）

| ステップ | 内容 |
|---|---|
| 1 | 9章「開発用サンプルデータ」パッケージで、2048小節・8パート・全奏法記号を含むフィクスチャを生成 |
| 2 | [[09_nonfunctional.md#1]]の各パフォーマンス目標値（起動/入力反応/fps/再生レイテンシ）を実測（0節の品質KPI一覧参照） |
| 3 | [[09_nonfunctional.md#3]]のメモリ目標値を編集シナリオ別（アイドル/通常/大曲）に実測（同上） |
| 4 | 目標未達の項目があれば、該当設計書（05〜06章、09章）にフィードバックし対策案を追記する（0.2節の通り、目標値自体は下げない） |

負荷テストは1節の4段階のうちシステムテスト〜運用テストの間に位置づけ、機能的な正しさではなく非機能要件の充足度を確認する横断的な軸として扱う。

## 9. 開発用サンプルデータ（要件9章、ユーザー向け同梱サンプルとは別）

- 全奏法記号（チョーキング／スライド／HP-PO／パームミュート／ビブラート／ハーモニクス3種／タッピング系）を最低1回ずつ含む短いフィクスチャ（機能網羅性テスト用）。
- 大曲負荷テスト用の2048小節フィクスチャ（機械生成でよい、実演奏として自然である必要はない）。
- 複数パート（8パート上限）・複数チューニング・カポ設定を含むフィクスチャ（パート管理・チューニング機能のテスト用）。
- これらは`tools/`パッケージ（[[01_architecture.md#3]]AD-5）のスクリプトとして生成し、リポジトリにコミットせず都度生成する運用とする（データサイズ・更新の手間を避けるため）。

## 10. 継続的インテグレーション（2026-09-02是正）

**2026-09-02是正**：本節は当初「一人開発・隙間時間開発という体制を踏まえ、大規模なCI基盤は組まず、コミット時にローカルで単体テストスイートを実行する軽量運用とする」としていたが、これは[[15_development_process.md#0]]で是正した通り誤りだった——CIはClaude Code・自動化ツールが実行する工程であり、本人の隙間時間を一切消費しないため、「一人開発だから」という理由で導入を見送る根拠が成立しない。[[15_development_process.md#2]]の是正内容に合わせ、本節も書き直す。

GitHub Actionsで、PRの作成・更新のたびに次を自動実行する：

- 2節のカバレッジ計測を含む単体テストスイート
- Lint（ESLint）・型チェック（`tsc --noEmit`）・Format確認（Prettier）

ブランチ保護ルールで上記のステータスチェックを必須化し、失敗している間は`main`へのマージをブロックする（[[15_development_process.md#3.1]]のPRベース運用と対応）。カバレッジ計測ツール（例：Vitestの`--coverage`）の出力をCIのステータスに反映し、2節の基準（0節の品質KPI一覧：C0/C1 100%、対象ロジックC2）を機械的に検証する。

負荷テスト（8節、実機での性能・メモリ計測）はCIでは自動化せず、Phase 0/1着手後に手動で実施する——起動時間・fps・メモリ使用量の実測にはElectronアプリを実際に起動する実機環境が必要であり、現状のCI環境（ヘッドレス実行が前提）では代替できないためである。将来的にElectronのヘッドレス実行環境が整えば自動化を検討するが、現時点ではスコープ外とする。
